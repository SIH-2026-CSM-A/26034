# Base URL the frontend calls. Unset falls back to http://localhost:8000.
#
# /api is the dev default: it routes through the Vite proxy in vite.config.ts,
# which keeps every call same-origin. A direct http://localhost:8000 works only
# once the backend serves CORS headers for the dev origin.
VITE_API_BASE_URL=/api
