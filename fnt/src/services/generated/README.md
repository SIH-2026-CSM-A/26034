# services/generated

Machine-written from the backend OpenAPI schema. **Never hand-edit anything in this
directory** — a regeneration will silently overwrite your changes, and any manual
fix belongs in the OpenAPI schema (`bck/app/main.py` / the FastAPI route definitions)
instead.

This directory is empty as of FNT-001 (route shell only, no API calls in scope). The
generation step — client + type generation from the running backend's `/openapi.json`
— lands in a later ticket.
